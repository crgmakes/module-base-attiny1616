# Aquarius - Automation Simplified
#
# @brief Git script for exporting PCB data for the repo.
# @note runs manually or automatically when there is a release
# @date 10/24/25
# @author cyberreefguru
# (c) CRG Makes 2025
name: Aquarius-Module-Base-1616-Release
on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  call-export-pcb:
    uses: crgmakes/kicad-extras/.github/workflows/crgm-ecad-release.yaml@main
    with:
      name: 'Aquarius Module Base 1616'
      repo-name: 'module-base-attiny1616'
      file-name: 'module-base-attiny1616'